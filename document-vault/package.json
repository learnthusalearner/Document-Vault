{
  "name": "document-vault",
  "version": "0.1.0",
  "description": "Document Vault — GraphQL API built with Bun, GraphQL Yoga, and Prisma",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "gendb": "bunx prisma generate && bunx prisma migrate dev"
  },
  "dependencies": {
    "@graphql-tools/schema": "^10.1.0",
    "@prisma/client": "^6.19.3",
    "graphql": "^16.14.2",
    "graphql-yoga": "^5.22.0",
    "prisma": "^6.19.3"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
