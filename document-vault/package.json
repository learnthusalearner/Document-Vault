{
  "name": "document-vault",
  "version": "0.1.0",
  "description": "Document Vault — GraphQL API built with Bun, GraphQL Yoga, and Prisma",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun x prisma generate",
    "postinstall": "bun x prisma generate",
    "vercel-build": "bun x prisma generate",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "gendb": "bun x prisma generate && bun x prisma migrate dev",
    "sanity": "bun run lint && bun run typecheck && bun test"
  },
  "dependencies": {
    "@graphql-tools/schema": "^10.1.0",
    "@prisma/client": "^6.19.3",
    "graphql": "^16.14.2",
    "graphql-yoga": "^5.22.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^26.2.0",
    "@vercel/node": "^7.0.0",
    "prisma": "^6.19.3",
    "typescript": "^5.7.3"
  }
}
